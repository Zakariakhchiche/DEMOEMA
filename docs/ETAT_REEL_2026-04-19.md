# ÉTAT RÉEL — 2026-04-19 (RÉVISION MAJEURE après consultation du repo Git)

> ⚠️ **CECI REMPLACE `ETAT_REEL_2026-04-17.md`** qui affirmait à tort "0 source branchée, démo seule".
>
> **Source** : repo `github.com/Zakariakhchiche/DEMOEMA` consulté le 2026-04-19.
>
> **Le projet est en réalité en PRODUCTION** — pas en phase idéation. Toute la documentation locale doit être mise à jour pour refléter cette réalité.

---

## 🔴 CORRECTION MAJEURE vs DOC PRÉCÉDENTE

La documentation `ETAT_REEL_2026-04-17.md` + `REPONSES_SELF_CHALLENGE.md` affirmait :
> "Rien n'est fait. 25 sources documentées / 0 branchée. Démo Vercel seule."

**C'est faux.** Le repo Git révèle un **produit en prod avec 7 modules opérationnels** depuis mars 2026.

---

## 1. Produit — État réel par module (source : README.md commit du 17 avril 2026)

| Module | Statut | Notes |
|---|---|---|
| **Dashboard** | ✅ **PROD** | Métriques temps réel, filtres sectoriels |
| **Intelligence Targets** | ✅ **PROD** | 200-300 cibles M&A, CSV export 19 colonnes, import NAF on-demand |
| **Feed Signaux** | ✅ **PROD** | 103 signaux M&A, filtres sévérité + 5 dimensions (Patrimoniaux / Stratégiques / Financiers / Gouvernance / Marché) |
| **Graphe Réseau** | ✅ **PROD** | ForceGraph2D glassmorphism, dirigeants + mandats croisés, nœuds colorés par score, GraphErrorBoundary |
| **Copilot IA** | ✅ **PROD** | Streaming SSE word-by-word, PDF report, recherche SIREN, multi-turn, fallback /query |
| **PWA / Mobile** | ✅ **PROD** | Manifest + Service Worker + SplashScreen Lottie radar + notifications push + install prompt |
| **Pipeline Data** | ✅ **PROD** | **62 profils NAF + BODACC hot SIRENs = 300+ cibles** |
| **Sweep 16M INSEE** | 🔧 **DEV EN COURS** | `sirene_bulk.py` committé 17/04, passer de 300 à 50k+ cibles |

**Nom du produit dans le repo : `EdRCF 6.0` — AI Origination Intelligence** (pas "DEMOEMA").

---

## 2. Stack technique réelle (source : repo Git)

| Couche | Stack réelle (en prod) | Stack documentée localement | Écart |
|---|---|---|---|
| Frontend | **Next.js 15** (React 19, TypeScript, Tailwind v4, Framer Motion) | Next.js 14 | ⚠️ Version différente |
| Backend | **FastAPI (Python 3.11)** + httpx async | FastAPI | ✅ OK |
| Database | **Supabase (Postgres managed + auth intégrée)** | Postgres self-hosted Scaleway + Supabase Auth | 🔴 **Stack cible diverge** |
| Hosting | **Vercel** (frontend + Python ASGI serverless) | Scaleway + Vercel | 🔴 **Pas Scaleway** |
| Orchestration | ~~Dagster~~ → **Vercel Cron** (cron schedule 0 3 * * *) | Dagster OSS Y1 | 🔴 **Pas Dagster** |
| Transformation | ~~dbt-core~~ → **Python direct + SQL Supabase** | dbt-core | 🔴 **Pas dbt** |
| Graphe | **ForceGraph2D (lib front)** + canvas custom | Neo4j Y3 | ✅ Cohérent (pas Neo4j Y1) |
| Vector | - | pgvector Y2 | ❓ Non implémenté |
| LLM | **Anthropic API (Claude)** streaming SSE | Claude + Mistral | ⚠️ Mistral pas intégré |
| Tests | **vitest + pytest (5 modules)** | Non spécifié | ✅ Meilleur que doc |
| Security | Next.js 15.2.3 (CVE-2025-66478 patché) | Non mentionné | ✅ Pro-actif |

**Décision stratégique de la doc locale "Scaleway strict FR" = NON APPLIQUÉE.** Le produit tourne sur Vercel + Supabase (AWS infrastructure). À trancher : revenir à la décision Scaleway ou accepter Vercel/Supabase ?

---

## 3. Sources data réellement branchées (source : `backend/data_sources.py`, `pappers_loader.py`, `sirene_bulk.py`)

### ✅ Sources actives en prod (au 17/04/2026)
1. **API Recherche Entreprises** (recherche-entreprises.api.gouv.fr) — identification + dirigeants
2. **INSEE SIRENE V3** (OAuth2) — SIREN/SIRET/NAF
3. **INSEE SIRENE Stock** (bulk Parquet) — **`sirene_bulk.py` committé 17/04**, pipeline 16M → 50k cibles
4. **INPI RNE** (OAuth2) — `fetch_inpi_rne()` avec gestion token, 10k req/jour gratuit
5. **BODACC** — `load_bodacc_hot_sirens()` → +25 pts aux SIRENs avec annonce récente
6. **Pappers** — `pappers_loader.py` (source d'enrichissement)
7. **API Recherche Entreprises** — `enrich_with_cross_mandates()` pour détection mandats croisés

### ✅ Autres sources utilisées (non exhaustif, à vérifier dans code)
- Scoring M&A propriétaire : 103 signaux répartis sur 12 dimensions
- Data enrichment : INPI RNE avec graceful fallback si credentials absents

**Écart avec la doc locale** : la doc dit "5 sources Q3 2026". **La réalité : 7+ sources déjà branchées, commit du 17/04 ajoute SIRENE bulk 16M.**

---

## 4. Endpoints API réels (source : `main.py`)

### En production
| Endpoint | Description |
|---|---|
| `GET /api/targets` | Cibles M&A depuis Supabase |
| `GET /api/signals` | Feed signaux scorés |
| `GET /api/graph` | Graphe réseau (nodes + links, mandats croisés, nœuds dirigeants) |
| `GET /api/search?q=` | Recherche entreprise par nom/SIREN |
| `POST /api/copilot` | Copilot IA (query unique) |
| `GET /api/copilot/stream` | **SSE streaming** word-by-word |
| `GET /api/admin/refresh-db?count=N` | Rechargement pipeline complet |
| `GET /api/admin/load-sector?naf=X&count=N` | Import on-demand secteur NAF |
| `GET /api/cron/refresh` | Vercel Cron — refresh quotidien 3h du matin |

### En développement (roadmap 16M)
| Endpoint | Description |
|---|---|
| `GET /api/admin/index-stats` | Stats sirene_index (total/enriched/hot) |
| `GET /api/admin/rebuild-index` | Lance `sirene_bulk.py` en background (mode `api` Vercel-safe ou `bulk` local) |
| `GET /api/admin/enrich-batch?n=20` | Enrichit N entreprises depuis sirene_index |

---

## 5. Commerce — à confirmer

La doc locale dit : "0 client, 0 revenu, 2 interviews, démo a plu".

**À vérifier** avec le founder :
- Les 300+ cibles en base sont-elles consultées par des utilisateurs (internes ? bêta ?) ?
- Le produit est-il visible publiquement sur `demoema.vercel.app` ou derrière auth ?
- Des boutiques M&A ont-elles accès à la prod ?
- LOI signées ?

**Hypothèse (à valider)** : produit en mode **dogfooding** par le founder, pas encore ouvert à des clients externes.

---

## 6. Équipe — à confirmer

La doc locale dit : "1 founder seul".

**Commits récents** : tous par `Claude Sonnet 4.6` en co-author avec le founder. Pas d'autres contributeurs humains dans l'historique git récent.

**Hypothèse confirmée** : founder seul, avec assistance IA (Claude Code) pour le dev.

---

## 7. Conformité RGPD / AI Act — à vérifier

La doc locale dit : "DPO pas contracté, LIA pas documentée, DPIA planifiée Q3 2026".

**À vérifier dans le code** :
- INPI RNE OAuth2 → données dirigeants = art. 14 RGPD (information indirecte) : page transparence en place ?
- Copilot streaming Claude → logs prompts ? watermarking ? mode "sans IA" ?
- Mentions légales / CGU : présentes sur le site ?

---

## 8. Implications stratégiques de cette découverte

### 🔴 La narrative "pré-MVP / Q1 perdu" n'est pas vraie

Ce qui a été documenté dans les audits SELF_CHALLENGE :
> "Rien n'est fait, calendrier décale +3 mois, closing pré-amorçage fin juin, MVP V1 livré décembre 2026."

**Réalité** : le MVP V1 est **DÉJÀ EN PROD** depuis mars 2026. Le founder est au stade **"scaling MVP → 50k cibles + traction commerciale"**, pas "construction MVP".

### 🎯 Repositionnement du pitch investisseur nécessaire

**Le deck devrait pitcher** :
- ✅ 7 modules en prod (pas "roadmap Q3-Q4")
- ✅ 300+ cibles M&A en base
- ✅ 103 signaux M&A propriétaires
- ✅ Copilot IA streaming (pas en Y2)
- ✅ PWA mobile en prod
- ✅ Pipeline SIRENE 16M en dev (prochain jalon)

**L'ask 600k€ pré-amorçage** devient :
- **Origine** : product-market validation + 50k cibles + GTM
- **Pas** : "construire le produit from scratch"

### 💰 Pricing et ARR cible révisés

Si le produit est déjà en prod, le founder peut :
- Ouvrir une **bêta payante** dès **maintenant** (pas Q4 2026)
- Générer du **revenue Q2-Q3 2026** (pas Y2+)
- Tripler potentiellement l'ARR Y1 (de 80k€ à 200-300k€) si le produit est commercialisé immédiatement

### 🏗️ Stack technique : Vercel/Supabase vs Scaleway strict FR

**Décision stratégique actuelle** (DECISIONS_VALIDEES.md) : "Souveraineté Scaleway strict FR".
**Réalité** : Vercel (US infra) + Supabase (AWS) = pas FR strict.

**À trancher** :
- **Option A** : Migrer vers Scaleway (effort : 2-4 semaines + risque prod)
- **Option B** : Assumer Vercel/Supabase Y1 + migration souveraine Y2 (plus pragmatique)
- **Option C** : Scaleway pour le data layer, Vercel pour le frontend (hybride)

**Impact commercial** : un fonds PE ou banque FR demandera la souveraineté → **Option A ou C nécessaire avant Enterprise**.

---

## 9. Docs à corriger immédiatement (propagation)

| Doc | Action |
|---|---|
| `ETAT_REEL_2026-04-17.md` | Marquer "OBSOLÈTE, voir ETAT_REEL_2026-04-19.md" |
| `REPONSES_SELF_CHALLENGE.md` | Corriger A1 "RIEN n'est fait" → "Produit en prod, 7 modules, 300+ cibles" |
| `ARCHITECTURE_TECHNIQUE.md` | Corriger stack "Postgres + Dagster + DuckDB" → "Supabase + FastAPI + Vercel + Python direct" |
| `DECISIONS_VALIDEES.md` | Trancher stack : souveraineté stricte Scaleway (plan migration) OU pragmatique Vercel/Supabase |
| `PLAN_DEMARRAGE_Q2-Q4_2026.md` | Réviser sprints : sweep 16M + commercialisation + migration (pas "build MVP") |
| `PITCH_DECK.md` Slide 9 Traction | Mettre à jour : "7 modules prod, 300 cibles, 103 signaux, Copilot IA" |
| `BROCHURE_COMMERCIALE.md` | Marquer features ✅ **déjà livrées** (plus de 🏗️ Q3 2026) sur Dashboard, Targets, Signaux, Graphe, Copilot, PWA |
| `ROADMAP_4ANS.md` | Avancer le calendrier Y1 → features livrées dès Q2 (pas Q4) |
| `FINANCES_UNIFIE.md` | Si revenue démarre Q2 vs Q4 → ARR Y1 + runway recalculés |
| `MODELE_FINANCIER.md` | Idem + éventuelle migration Scaleway budgétée |
| `ARCHITECTURE_DATA_V2.md` | Marquer sources ✅ "en prod" sur #1, #2, #3, #4, #30 (partiel) |

---

## 10. Questions critiques pour le founder

1. **Quel est le statut commercial réel** ? Combien d'utilisateurs actifs ? Prospects ? LOI ?
2. **Le produit est-il ouvert publiquement** ou derrière auth ?
3. **Pourquoi la doc locale affirme "0 branché"** alors que le produit est en prod ?
4. **La décision Scaleway** est-elle maintenue ou pivotée vers Vercel/Supabase ?
5. **Les 600k€ pré-amorçage** sont-ils toujours justifiés au même niveau ou peut-on viser une levée plus grosse vu la traction produit ?
6. **Le pitch actuel** raconte une histoire de "construction" alors que la réalité est "scaling" : lequel est le vrai narratif ?

---

## 11. Recommandation urgente

**Stopper toute production doc tant que le statut réel n'est pas validé par le founder.**

Les 25+ documents dans `docs/` racontent une histoire (projet au stade pré-MVP) qui **contredit directement le repo Git** (produit en prod 7 modules, 300 cibles, Copilot IA streaming).

**3 options pour l'agent documentation** :
1. **Tout refondre** en partant de la réalité git (6-8h de travail)
2. **Ajouter une bannière "V4"** à chaque doc disant "état pré-MVP dépassé, voir ETAT_REEL_2026-04-19"
3. **Consolider en 1 nouveau doc maître** : `PROJET_REEL_V4.md` qui remplace tout le narratif actuel

---

_Document créé le 2026-04-19 après consultation du repo github.com/Zakariakhchiche/DEMOEMA (main branch, dernier push 2026-04-18)._
