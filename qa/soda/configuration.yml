data_source demoema:
  type: postgres
  connection:
    host: ${POSTGRES_HOST}
    port: ${POSTGRES_PORT}
    database: ${POSTGRES_DB}
    username: ${POSTGRES_USER_RO}
    password: ${POSTGRES_PASSWORD_RO}
  schema: silver
