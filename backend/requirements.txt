# Core FastAPI stack — versions bumped pour fixer CVE (audit SEC-11)
fastapi==0.137.1
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
httpx==0.28.1
starlette==0.52.1
python-multipart==0.0.32
# Sécurité parsing XML (audit SEC-3) : protège Google News RSS, BODACC, SIRENE
defusedxml==0.7.1

# Tests
pytest==8.4.2
pytest-asyncio==0.26.0
anyio==4.14.0

# Data / Pipelines
duckdb>=0.10.3
psycopg2-binary>=2.9.9
asyncpg==0.31.0
# Neo4j graph driver — endpoint /api/graph/person/{nom}/{prenom} pour
# lire les flags + co-mandataires depuis le graphe complet 18.6M nodes.
neo4j==5.28.4

# Observability
python-json-logger==2.0.7
prometheus-fastapi-instrumentator==7.1.0
slowapi==0.1.10

# PDF generation (Bug S rapport QA — endpoint /pitch/{siren}.pdf)
weasyprint>=63.0

# Cache partagé multi-worker (scalabilité 100 users). Fallback in-memory si indispo.
redis>=5.0
